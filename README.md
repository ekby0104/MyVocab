# MyVocab 📚

> **⚠️ Disclaimer**
>
> 이 앱은 **개인 학습 목적**으로 제작된 **비공식** 단어장 학습 앱입니다.
> 네이버 및 NAVER Corp.와 어떠한 제휴 관계도 없으며, 네이버 공식 API가 아닙니다.
> 네이버 연동 기능은 본인 계정의 본인 데이터를 개인 학습 목적으로만 사용하도록 설계되었습니다.
> 이 코드를 사용하거나 수정하는 것에 대한 책임은 사용자 본인에게 있습니다.
>
> This is a **personal learning project** and is **not affiliated** with NAVER Corp.
> It is not an official Naver application. The Naver integration feature is designed
> to access the user's own data for personal learning purposes only.

---

네이버 단어장과 연동되는 iOS 단어 학습 앱. SwiftUI + SwiftData 기반.
**iPhone / iPad · 세로/가로 모드 모두 지원**

## ✨ 주요 기능

### 📖 단어 관리
- **전체 단어 / 즐겨찾기 / 검색** 탭 분리
- **7가지 정렬**: 최신순, 알파벳, 알파벳 역순, 즐겨찾기 우선, 오답 순, **복습 임박순**, 랜덤
- **뜻 보이기/숨기기**: 목록에서 뜻을 숨겨서 셀프 테스트 가능
- **스와이프 액션**: 즐겨찾기 토글 + 삭제
- **단어 직접 추가/편집**: 영어, 발음, 품사, 뜻, 예문, 메모 전부 편집 가능
- **단어 상세 화면**: 학습 통계, 메모, TTS, 개별 학습 기록 관리
- **모든 리스트의 별 아이콘으로 즐겨찾기 토글** (전체/즐겨찾기/검색/통계 어디서든)
- **복습 일자 표시**: 각 단어 셀에 다음 복습일 라벨
  - 🟢 **복습 가능** (도래)
  - 🔵 **N시간 N분 후** (오늘 중 - 파란색 강조)
  - 🟡 **내일** (노란색)
  - ⚪ N일 후 / N주 후 (회색)
  - 🟢 **NEW** (미학습)

### 🔍 강력한 검색
- **한/영 통합 검색**: 영어, 한글 뜻, 발음, 예문 모두 검색 대상
- **검색 범위 선택**: 전체 / 단어 / 예문 필터
- **와일드카드 지원**:
  - `ab*` — ab로 시작 (0개 이상)
  - `*ing` — ing로 끝 (0개 이상)
  - `c*t` — c와 t 사이에 0개 이상
  - `c?t` — c+한글자+t (cat, cot, cut)
- **500ms 디바운스 검색**: 타이핑 끝나면 자동 검색
- **자동 키보드 활성화**: 검색 탭 첫 진입 시 키보드 자동 노출
- **검색 기록**: 최근 20개 자동 저장, 탭으로 재검색, 개별/전체 삭제
- **일괄 즐겨찾기**: 검색 결과 전체를 한 번에 즐겨찾기

### 🎮 학습 모드 (게임 탭에서 시작)
모든 게임은 시작 전에 **학습 대상**을 선택할 수 있습니다:
- 📚 전체 단어
- ⭐ 즐겨찾기
- 🔄 틀린 단어
- 📅 **오늘의 학습** (SRS 기반, 기본 선택)
- 📊 **레벨별** (Lv.0~7 체크박스로 다중 선택)

#### 🎴 플래시카드
- 앞면(영어) ↔ 뒷면(뜻/예문) 탭으로 전환 (일관된 페이드 효과)
- 🔊 수동 TTS 버튼 + 자동 발음 토글
- ℹ️ 카드 우상단 정보 버튼 → 단어 상세로 이동
- 섞기, 다시 선택 메뉴

#### 🎯 퀴즈 (6지선다)
- 영→한 / 한→영 모드 선택
- ⏱ **사용자 지정 시간**: 5초~30초 (5초 단위, 기본 10초)
- **자동 오답 처리**: 시간 내에 답하지 않으면 자동 오답
- **건너뛰기 버튼**: 답을 모를 때 즉시 오답 처리하고 다음 문제로
- **다음 문제 버튼 항상 표시** (카드 위치 고정)
- **가로 모드 전용 레이아웃**: 좌측 문제 카드 + 우측 2×3 옵션 그리드
- ℹ️ 답 처리 후 정보 버튼 노출 (단어 상세 이동)
- SRS 자동 반영 (정답률 기반 레벨 변경)

#### 🔀 단어 매칭
- 영어 8장 + 한글 8장 = 16장 카드 (4×4 / 가로 모드 8×2 그리드)
- ⏱ **사용자 지정 시간**: 10초~2분 (10초 단위, 기본 30초)
- ⏸ **포기 버튼**: 게임 도중 종료 가능 (가로 모드는 X 버튼)
- 같은 종류(영-영, 한-한) 선택 방지
- 매칭 성공 → 초록 깜빡, 실패 → 빨간 깜빡
- **첫 시도 정답만 정답 처리**: 한 번이라도 틀린 단어는 오답으로 기록
- 시간 초과/포기 시 미매칭 단어 모두 오답 처리
- **게임 종료 시 일괄 SRS 처리**: 게임 중 DB 쓰기 없어 빠른 반응
- 결과 화면: 못 맞춘 단어 + 맞춘 단어 리스트 분리 표시

### 📅 간격 반복 학습 (SRS)
Leitner box 방식의 8단계 레벨 시스템 + 정답률 반영:

**레벨별 복습 간격** (레벨 = 일수):
| 레벨 | 복습 간격 |
|------|----------|
| 0 | 즉시 (오답) |
| 1 | 1일 후 |
| 2 | 2일 후 |
| 3 | 3일 후 |
| 4 | 4일 후 |
| 5 | 5일 후 |
| 6 | 6일 후 |
| 7 | 7일 후 (마스터) |

**레벨 변경 규칙 (정답률 반영)**:
- 정답 시 정답률 ≥ 50% → 레벨 +1
- 정답 시 정답률 < 50% → 레벨 그대로 (실력이 충분치 않으면 올리지 않음)
- 오답 시 정답률 ≥ 30% → 레벨 -1 (한 단계만 내림)
- 오답 시 정답률 < 30% → 레벨 0 (완전 리셋)

**다음 복습 일자 (정답률 반영)**:
- 정답률 ≥ 70% → 원래 간격 100%
- 정답률 50~70% → 원래 간격 75%
- 정답률 < 50% → 원래 간격 50%
- 오답 시 → **4시간 후** 복습 (정답률 무관)

### 📊 학습 통계
- **오늘의 학습 카드**: 오늘 복습 가능한 단어 수 표시 (가장 위)
- **요약 도넛 카드**: 정답률 + 총 단어 / 학습 완료 / 마스터 / 즐겨찾기 / 틀린 단어
- **학습 누적 바**: 정답/오답 비율 시각화
- **SRS 레벨 분포**: 레벨별 단어 수 막대 그래프
- **자주 틀리는 단어 TOP 10**: 순위와 오답 횟수 표시, 탭하면 상세로 이동

### 🔊 TTS 발음
- `AVSpeechSynthesizer` 기반 영어 발음 (미국식, 학습용 속도 0.45x)
- **딜레이 최적화**: voice 인스턴스 캐싱 + 즉시 워밍업 + spokenAudio 모드
- 단어 상세: 단어 + 예문 각각 발음 버튼
- 플래시카드: 수동 버튼 + 자동 발음 토글
- 퀴즈: 영→한 모드에서 발음 버튼
- 단어 편집: 입력 중 발음 미리듣기

### 🔐 네이버 동기화
- **WebView 기반 로그인**: 네이버 공식 로그인 페이지 사용
- **쿠키 영구 저장**: 한 번 로그인 후 만료 전까지 자동 유지
- **빠른 동기화**: 탭 한 번으로 전체 단어 업데이트
- **동적 단어장 목록**: API에서 단어장 목록 자동 조회
- **세션 만료 자동 감지**: 만료 시 재로그인 유도
- **강화된 파싱**: 다양한 응답 키 위치 시도 + HTML 엔티티 디코딩 (`&#124;` 등 숫자 엔티티 포함)

### 🛡 학습 기록 관리 (3단계)

**단어 상세 (개별)** — 가로 스크롤 가능:
| 버튼 | 동작 |
|------|------|
| 정답 카운트 초기화 | correctCount만 0 |
| 오답 카운트 초기화 | wrongCount만 0 |
| 틀린 단어 해제 | isWrong만 false |
| 전체 초기화 | 정답·오답·SRS·틀린 상태 전부 리셋 |

**게임 탭 → 관리 (전체)**:
| 버튼 | 동작 |
|------|------|
| 틀린 단어 전체 해제 | 모든 단어 isWrong → false |
| 오답 기록 전체 초기화 | 모든 단어 wrongCount → 0 |
| 학습 기록 전체 초기화 | 모든 단어 정답·오답·SRS·틀린 상태 전부 리셋 |

### 💾 백업 / 복원 (v2)
백업되는 데이터:
- 단어 기본 정보 (영어, 발음, 품사, 뜻, 예문, 한글 예문)
- 사용자 데이터 (메모, 즐겨찾기, 틀린 단어 표시)
- 학습 통계 (정답/오답 카운트, 마지막 학습 시각)
- **SRS 정보 (v2)**: srsLevel, nextReviewDate

기능:
- **공유 시트**: iCloud Drive, 파일 앱 등에 저장
- **병합 복원**: 기존 단어 보존 + 백업 데이터로 메타데이터 복원
- **하위 호환**: v1 백업도 그대로 복원 가능

### 📥 JSON Import
- 네이버 원본 API 응답 파싱 (`data.m_items` 중첩 구조)
- 단순 배열 포맷 지원
- HTML 태그, 명명·숫자 엔티티 자동 정리
- 중복 감지 + 건너뛴 사유별 상세 로그

### 🎯 네비게이션 동작
- **전체/즐겨찾기 탭 재탭**: 단어 상세에서 빠져나오기 (pop-to-root)
- **검색 탭 재탭** (1단계): 단어 상세에서 빠져나오기
- **검색 탭 재탭** (2단계): 검색어/범위 초기화
- **게임 탭 재탭**: 게임 시작 화면으로 복귀
- **탭 간 이동 시 단어 상세 유지**: 다른 탭 갔다 와도 그대로 보임

## 🛠 기술 스택

- **SwiftUI** — 전체 UI (iOS 17+)
- **SwiftData** — 단어 저장소 (`@Model`, `@Query`)
- **WKWebView** — 네이버 로그인
- **URLSession** — 쿠키 기반 API 호출
- **AVSpeechSynthesizer** — TTS
- **Combine** — 타이머 기반 자동 오답 처리
- **UserDefaults / @AppStorage** — 쿠키, 검색 기록, 단어장 목록, 게임 시간 설정 저장

## ⚡ 성능 최적화

- **Lazy 탭 패턴**: 탭이 처음 열릴 때만 뷰 생성, `@Query`가 4개 동시 활성화되는 것 방지
- **@State 캐싱**: WordListView/GameView/StatsView/SearchView 모두 `cachedList` 등 `@State`로 결과 캐시 + `rebuild()` 함수
- **onChange 최적화**: `words` 대신 `words.count`로 트리거 (어떤 속성 변경에도 fire되는 것 방지)
- **검색 디바운스**: 500ms 후 검색 실행 (타이핑 중 매번 검색 X)
- **메모 저장**: 포커스 해제 시에만 저장 (글자마다 save 안 함)
- **매칭 게임 SRS 일괄 처리**: 게임 종료 시 한 번만 save (매칭 시도마다 X)
- **빈 화면 보호**: SwiftData 비동기 로딩 중 race condition 방지
- **이중 onChange**: `words.count`와 `words` 모두 감지해서 초기 로딩 안정화
- **TTS 딜레이 감소**: voice 인스턴스 캐싱 + 즉시 워밍업

## 📁 프로젝트 구조

```
MyVocab/
├── App/
│   └── MyVocabApp.swift            // 진입점 + URL 스킴 + 자동 DB 복구
├── Models/
│   └── Word.swift                  // SwiftData 모델 (SRS 필드 포함)
├── Services/
│   ├── NaverImporter.swift         // 네이버 JSON 파서 (다양한 키 위치 + 엔티티 디코딩)
│   ├── NaverSync.swift             // 네이버 API + 쿠키/단어장 저장
│   ├── SpeechService.swift         // TTS 래퍼 (voice 캐싱)
│   ├── SRSService.swift            // Leitner box SRS + 정답률 반영
│   └── BackupService.swift         // 백업/복원 v2 (SRS 포함)
├── Theme/
│   └── Theme.swift                 // 디자인 토큰 (VocabChip Kind: neutral/wrong/favorite/correct/info)
└── Views/
    ├── RootTabView.swift           // 5탭 네비게이션 (lazy + 가로 모드 축소)
    ├── WordListView.swift          // 전체/즐겨찾기 (7가지 정렬, 복습일 라벨)
    ├── WordDetailView.swift        // 상세 + TTS + 편집 + 학습 기록 관리 (가로 스크롤)
    ├── WordEditView.swift          // 단어 추가/편집 폼
    ├── SearchView.swift            // 와일드카드 검색 + 디바운스 + 자동 포커스
    ├── GameView.swift              // 학습 모드 허브 + 전체 학습 관리
    ├── FlashcardView.swift         // 플래시카드 (페이드 전환 + 정보 버튼)
    ├── QuizView.swift              // 6지선다 퀴즈 (시간 선택 + 가로 모드)
    ├── MatchingGameView.swift      // 단어 매칭 (10초~2분 + 가로/세로 그리드)
    ├── StatsView.swift             // 통계 대시보드 (오늘의 학습 + 도넛 + 레벨 분포)
    ├── NaverSyncView.swift         // 네이버 WebView 로그인
    ├── SettingsView.swift          // 설정 (섹션 분리)
    └── ShareSheet.swift            // UIActivityViewController 래퍼
```

## 🚀 시작하기

### 요구사항
- macOS 14+
- Xcode 15+
- iOS 17+ 실기기 또는 시뮬레이터

### 빌드
1. 이 저장소를 clone
2. `MyVocab.xcodeproj` 열기
3. Signing & Capabilities에서 본인 Apple ID 선택
4. ⌘R로 실행

### 첫 사용 흐름
1. **설정 → 네이버 로그인** → WebView에서 네이버 로그인
2. **단어장 목록 새로고침** → 본인의 단어장 목록 자동 로드
3. **단어장 선택** → 드롭다운에서 원하는 단어장
4. **빠른 동기화** → 단어 가져오기
5. **게임 탭** → 플래시카드 / 퀴즈 / 매칭 선택 → 학습 시작!

## ⚠️ 주의사항

- 네이버 공식 API가 아니므로 네이버 측 변경에 따라 작동이 중단될 수 있습니다
- 무료 Apple 개발자 계정 사용 시 7일마다 재설치 필요
- SwiftData 모델 변경 시 자동으로 DB 초기화됨 (앱은 계속 동작)
- **데이터 보존이 중요하면 정기적으로 백업 파일 만들기 권장**

## 🗺 로드맵

### ✅ 완료
- **Phase 1**: TTS + 단어 추가/편집
- **Phase 2-A**: SRS + 통계 + 백업/복원
- **Phase 2-B**: 오늘의 복습 + 검색 기록
- **Phase 3**: 단어 매칭 게임 + 자동 오답 + 학습 기록 3단계 관리
- **Phase 4**: UI 통합 - 모든 게임 시작 화면에서 소스 선택 (전체/즐겨찾기/틀린 단어/오늘의 학습)
- **Phase 5**: 성능 최적화 (Lazy 탭, @State 캐싱, 디바운스 검색)
- **Phase 6**: 가로 모드 지원 (퀴즈 좌우 분할, 매칭 8×2, 탭바 축소)
- **Phase 7**: 네비게이션 개선 (탭 재탭 동작, 단어 상세 유지)
- **Phase 8**: 백업 v2 (SRS 정보 포함), 매칭 게임 시간 커스텀 (10초~2분)
- **Phase 9**: SRS 정답률 반영 (레벨 변경 + 복습 간격 단축), 오답 시 4시간 후
- **Phase 10**: WordListView 복습 임박순 정렬 + 복습일 라벨 (오늘 중 파란색, 내일 노란색)
- **Phase 11**: 게임 소스에 "레벨별" 추가 (체크박스 다중 선택, 좌측 정렬)
- **Phase 12**: 퀴즈 시간 선택 (5초~30초), 문제 수 제거, 정보 버튼 추가
- **Phase 13**: 플래시카드 페이드 전환 일관성, TTS 딜레이 최적화

### 💭 아이디어
- 🔔 학습 알림 (매일 정해진 시간 푸시)
- ⌨️ 타자 입력 퀴즈 (스펠링 학습)
- 🏷 태그/카테고리 시스템
- 🔥 연속 학습일 (스트릭)
- 📈 주간/월간 리포트
- 🌙 다크모드 강제 토글
- 📱 홈 화면 위젯 (유료 개발자 계정 필요)

## License

MIT License — 개인 학습용 프로젝트
